{
  "version": 2,
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "regions": ["iad1"],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
